stages:
  - build
  - test
  - deploy

build:
  stage: build
  script:
    - python ./test_example.py

# build_app:
#   stage: build
#   script:
#     - python ./app.py

# build_ruff:
#   stage: build
#   script:
#     - ruff check --output-format=gitlab --output-file=puff.toml
    #code-quality-report.json

test:
  stage: test
  script:
    - python -m pytest test_example.py

# deploy_pack:
#   stage: deploy
#   tags: ["python"]
#   script:
#     - mkdir -p public
#     - cp app.py public/
#   artifacts:
#     paths:
#       - public
#   only:
#     - main

deploy:
  stage: deploy
  GIT_STRATEGY: none
  script:
    - rsync -av app.py tatiyana.pythonanywhere.com
  only:
    - main
  tags:
    - python

#$CI_PROJECT_URL - URL проекта